<%= form_for(@sidecla) do |f| %>
    <fieldset>
      <% if @sidecla.errors.any? %>
          <div id="error_explanation">
            <h2><%= pluralize(@sidecla.errors.count, "error") %> prohibited this sidecontent from being saved:</h2>
            <ul>
              <% @sidecla.errors.full_messages.each do |message| %>
                  <li><%= message %></li>
              <% end %>
            </ul>
          </div>
      <% end %>
      <table class="col-lg-12">
        <tr>
          <div  class="form-group">
            <td style="margin-top: 10px;width:90px;text-align: right;padding: 10px;"> <%= f.label :名称 %></td>
            <td><%= f.text_field :name ,:class =>"form-control"%></td>
          </div>
        </tr>
        <tr>
          <td align="right" colspan="2" style="padding: 0px;"><div class="actions">
            <%= f.submit :"确定", class:"btn btn-primary-outline pull-right" %></td>
          </div>
        </tr>
      </table>
    </fieldset>
<% end %>
